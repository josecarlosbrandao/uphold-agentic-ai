---
allowed-tools: Skill
description: Commit, push, and open a PR
---

## Your task

This command chains `create-commit` and `create-pr`:

1. Use Skill tool to invoke `create-commit`
2. After commit is pushed, use Skill tool to invoke `create-pr`

Both skills will handle their own user interactions and provide their own feedback.
