---
description: Create mermaid diagrams
disable-model-invocation: true
---

## Arguments

- `$SCOPE`: The entrypoint where you should start (first participant)
- `$FILE`: The `.mmd` file containing the mermaid diagram to be exported.

## Your task

1. Create a `.mmd` file making sure to follow all possible execution flows and participants, but keep it relevant and omit external services, persistence layer as well as domain logic (utils, etc) unless explicitly asked
